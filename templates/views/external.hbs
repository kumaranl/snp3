<div class="container">

  <div class = "row">
      {{# each Externals }}
    <div class="col-md-3">
        {{{shortdesc}}}<br>
          <a href = "{{path}}" target="_blank"><img src="{{imagepath}}" class="rounded" alt="{{title}}" width="150" height="150"></a>
    </div>
    {{/each}}
  </div>
</div>
